server:
  port: 8101

spring:
  application:
    name: CMS-Student-Payment

  datasource:
    url: jdbc:mysql://localhost:3306/cms_payment
    username: root
    password: Dev@#$123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: update

student:
  uri:
    baseUrl: http://localhost:8104/
    getStudentById: api/v1/student/##STUDENT-ID##
    getAllStudentDetails: api/v1/student

location:
  uri:
    baseUrl: http://localhost:8105/
    getAllLocationDetails: api/v1/tuition/